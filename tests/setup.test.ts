/**
 * Basic test to verify that the Jest testing environment is set up correctly.
 */

describe('Testing setup', () => {
  test('Jest is working', () => {
    expect(1 + 1).toBe(2);
  });
});
